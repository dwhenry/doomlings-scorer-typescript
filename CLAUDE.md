This is a mono-repo for a scoring app for a card game.

The /web directory contains a basic React client application
The /lib directory has the base scoring logic in it

- Use DRY principles
- Ask questions to clarify if you are unsure
