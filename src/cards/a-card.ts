import { Card, addCard } from "../cardContainer"

const acrobatic: Card = {
  name: 'ACROBATIC',
  colour: 'multi-colour',
  pointsA: 2,
};

addCard(acrobatic)

const adorable: Card = {
  name: 'ADORABLE',
  colour: 'purple',
  pointsA: 4,
};

addCard(adorable)
