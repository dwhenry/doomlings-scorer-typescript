import { Card, addCard } from "../cardContainer"

const acrobatic: Card = {
  name: 'ACROBATIC',
  type: 'multi-colour',
  pointsA: 2,
};

addCard(acrobatic)

const adorable: Card = {
  name: 'ADORABLE',
  type: 'purple',
  pointsA: 4,
};

addCard(adorable)
